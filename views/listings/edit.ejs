

 <% layout("/layouts/boilerplate") %>

<div class="row mt-3">
    <div class="col-8 offset-2">

    <h3>Edit New Listing</h3>
    <form method="post" action="/listings/<%= listing._id %>?_method=PUT " novalidate class="needs-validation" enctype="multipart/form-data">

    <div class="mb-3">
        <label for="title" class="form-label">Title</label>
        <input name="listing[title]" class="form-control" value="<%= listing.title %>" required>
        <div class="valid-feedback">Title looks good</div>
    </div>

    <div class="mb-3">
        <label for="description" class="form-label">Description</label>
        <input name="listing[description]" class="form-control" value="<%= listing.description %>" required>
        <div class="invalid-feedback">Enter minium 250 characters</div>
    </div>

   

    <div class="mb-3">
        <label for="image" class="form-label"></label>
        <img src="<%= listing.image.url %>" alt="Image Preview" class="img-thumbnail" style="max-width: 200px; max-height: 200px;">
    </div>

    <!-- ✅ FIXED LINE (was listing[img]) -->
    <div class="mb-3">
        <label for="image" class="form-label">Upload new Image</label>
        <input name="listing[image][url]" class="form-control" type="file">
    </div>

    <div class="row">
    <div class="mb-3 col-md-4">
        <label for="price" class="form-label">Price</label>
        <input name="listing[price]" class="form-control" value="<%= listing.price %>" required>
        <div class="invalid-feedback">Enter valid price</div>
    </div>

    <!-- ✅ FIXED (name was wrong) -->
    <div class="mb-3 col-md-8">
        <label for="country" class="form-label">Country</label>
        <input name="listing[country]" class="form-control" value="<%= listing.country %>" required>
        <div class="invalid-feedback">Enter country</div>
    </div>
    </div>

    <!-- ✅ FIXED (name was wrong) -->
    <div class="mb-3">
        <label for="location" class="form-label">Location</label>
        <input name="listing[location]" class="form-control" value="<%= listing.location %>" required>
        <div class="invalid-feedback">Enter correct Location</div>
    </div>

        <button class="btn btn-dark edit-btn mt-3" type="submit">Edit Listing!</button>
    </form><br>

    </div>
    </div>
<% layout("/layouts/boilerplate") %>

<div class="row mt-3">
    <div class="col-8 offset-2">

    <h3>Edit New Listing</h3>
    <form method="post" action="/listings/<%= listing._id %>?_method=PUT " novalidate class="needs-validation">

    <div class="mb-3">
        <label for="title" class="form-label">Title</label>
        <input name="listing[title]" class="form-control" value="<%= listing.title %>" required>
        <div class="valid-feedback">Title looks good</div>
    </div>

    <div class="mb-3">
        <label for="description" class="form-label">Description</label>
        <input name="listing[description]" class="form-control" value="<%= listing.description %>" required>
        <div class="invalid-feedback">Enter minium 250 characters</div>
    </div>

    <!-- ✅ FIXED LINE (was listing[img]) -->
    <div class="mb-3">
        <label for="image" class="form-label">Image</label>
        <input name="listing[image][url]" class="form-control" value="<%= listing.image.url %>">
    </div>

    <div class="row">
    <div class="mb-3 col-md-4">
        <label for="price" class="form-label">Price</label>
        <input name="listing[price]" class="form-control" value="<%= listing.price %>" required>
        <div class="invalid-feedback">Enter valid price</div>
    </div>

    <!-- ✅ FIXED (name was wrong) -->
    <div class="mb-3 col-md-8">
        <label for="country" class="form-label">Country</label>
        <input name="listing[country]" class="form-control" value="<%= listing.country %>" required>
        <div class="invalid-feedback">Enter country</div>
    </div>
    </div>

    <!-- ✅ FIXED (name was wrong) -->
    <div class="mb-3">
        <label for="location" class="form-label">Location</label>
        <input name="listing[location]" class="form-control" value="<%= listing.location %>" required>
        <div class="invalid-feedback">Enter correct Location</div>
    </div>

        <button class="btn btn-dark edit-btn mt-3" type="submit">Edit Listing!</button>
    </form><br>

    </div>
    </div>
